```sql
SELECT * FROM CandyData WHERE chocolate = 1 LIMIT 1;
```

```sql
INSERT INTO CandyData (competitorname, chocolate, fruity, caramel, peanutyalmondy, nougat, crispedricewafer, hard, bar, pluribus, sugarpercent, pricepercent, winpercent) VALUES ('Test Candy', 1, 0, 0, 0, 0, 1, 0, 1, 0, 0.75, 0.5, 60.0);
```

```sql
SELECT * FROM CandyData WHERE competitorname = 'Test Candy';
```

```sql
UPDATE CandyData SET winpercent = 80.0 WHERE competitorname = 'Test Candy';
```

```sql
DELETE FROM CandyData WHERE competitorname = 'Test Candy';
```

```sql
SELECT * FROM CandyData WHERE chocolate = 1 LIMIT 1;
```

```sql
INSERT INTO CandyData (competitorname, chocolate, fruity, caramel, peanutyalmondy, nougat, crispedricewafer, hard, bar, pluribus, sugarpercent, pricepercent, winpercent) VALUES ('Test Candy', 1, 0, 0, 0, 0, 1, 0, 1, 0, 0.75, 0.5, 60.0);
```

```sql
SELECT * FROM CandyData WHERE competitorname = 'Test Candy';
```

```sql
UPDATE CandyData SET winpercent = 80.0 WHERE competitorname = 'Test Candy';
```

```sql
DELETE FROM CandyData WHERE competitorname = 'Test Candy';
```

```sql
SELECT * FROM CandyData WHERE chocolate = 1 LIMIT 1;
```

```sql
INSERT INTO CandyData (competitorname, chocolate, fruity, caramel, peanutyalmondy, nougat, crispedricewafer, hard, bar, pluribus, sugarpercent, pricepercent, winpercent) VALUES ('Test Candy', 1, 0, 0, 0, 0, 1, 0, 1, 0, 0.75, 0.5, 60.0);
```

```sql
SELECT * FROM CandyData WHERE competitorname = 'Test Candy';
```

```sql
UPDATE CandyData SET winpercent = 80.0 WHERE competitorname = 'Test Candy';
```

```sql
DELETE FROM CandyData WHERE competitorname = 'Test Candy';
```

```sql
INSERT INTO CandyData (competitorname, chocolate, fruity, caramel, peanutyalmondy, nougat, crispedricewafer, hard, bar, pluribus, sugarpercent, pricepercent, winpercent) VALUES ('Test Candy', 1, 0, 0, 0, 0, 1, 0, 1, 0, 0.75, 0.5, 60.0);
```

```sql
SELECT * FROM CandyData WHERE competitorname = 'Test Candy';
```

```sql
UPDATE CandyData SET winpercent = 80.0 WHERE competitorname = 'Test Candy';
```

```sql
DELETE FROM CandyData WHERE competitorname = 'Test Candy';
```

```sql
SELECT * FROM CandyData WHERE chocolate = 1 LIMIT 1;
```

```sql
SELECT * FROM CandyData WHERE chocolate = 1 LIMIT 1;
```

```sql
INSERT INTO CandyData (competitorname, chocolate, fruity, caramel, peanutyalmondy, nougat, crispedricewafer, hard, bar, pluribus, sugarpercent, pricepercent, winpercent) VALUES ('Test Candy', 1, 0, 0, 0, 0, 1, 0, 1, 0, 0.75, 0.5, 60.0);
```

```sql
SELECT * FROM CandyData WHERE competitorname = 'Test Candy';
```

```sql
UPDATE CandyData SET winpercent = 80.0 WHERE competitorname = 'Test Candy';
```

```sql
DELETE FROM CandyData WHERE competitorname = 'Test Candy';
```

```sql
SELECT * FROM CandyData WHERE competitorname = 'Sample Candy';
```

```sql
INSERT INTO CandyData (competitorname, chocolate, fruity, caramel, peanutyalmondy, nougat, crispedricewafer, hard, bar, pluribus, sugarpercent, pricepercent, winpercent) VALUES ('Sample Candy', 1, 0, 1, 0, 0, 1, 0, 1, 0, 0.5, 0.3, 75.0);
```

```sql
INSERT INTO CandyData (competitorname, chocolate, fruity, caramel, peanutyalmondy, nougat, crispedricewafer, hard, bar, pluribus, sugarpercent, pricepercent, winpercent) VALUES ('Sample Candy', 1, 0, 1, 0, 0, 1, 0, 1, 0, 0.5, 0.3, 75.0);
```

```sql
SELECT * FROM CandyData WHERE competitorname = 'Sample Candy';
```

```sql
UPDATE CandyData SET winpercent=80.0 WHERE competitorname='Sample Candy';
```

```sql
DELETE FROM CandyData WHERE competitorname='Sample Candy';
```

```sql
INSERT INTO CandyData (competitorname, chocolate, fruity, caramel, peanutyalmondy, nougat, crispedricewafer, hard, bar, pluribus, sugarpercent, pricepercent, winpercent) VALUES ('Test Candy', 1, 0, 0, 0, 0, 1, 0, 1, 0, 0.75, 0.5, 60.0);
```

```sql
SELECT * FROM CandyData WHERE competitorname = 'Test Candy';
```

```sql
UPDATE CandyData SET winpercent = 80.0 WHERE competitorname = 'Test Candy';
```

```sql
DELETE FROM CandyData WHERE competitorname = 'Test Candy';
```

```sql
SELECT * FROM CandyData WHERE chocolate = 1 LIMIT 1;
```

```sql
INSERT INTO CandyData (competitorname, chocolate, fruity, caramel, peanutyalmondy, nougat, crispedricewafer, hard, bar, pluribus, sugarpercent, pricepercent, winpercent) VALUES ('Sample Candy', 1, 0, 1, 0, 0, 1, 0, 1, 0, 0.5, 0.3, 75.0);
```

```sql
SELECT * FROM CandyData WHERE competitorname = 'Sample Candy';
```

```sql
UPDATE CandyData SET winpercent=80.0 WHERE competitorname='Sample Candy';
```

```sql
DELETE FROM CandyData WHERE competitorname='Sample Candy';
```

```sql
INSERT INTO CandyData (competitorname, chocolate, fruity, caramel, peanutyalmondy, nougat, crispedricewafer, hard, bar, pluribus, sugarpercent, pricepercent, winpercent) VALUES ('Test Candy', 1, 0, 0, 0, 0, 1, 0, 1, 0, 0.75, 0.5, 60.0);
```

```sql
SELECT * FROM CandyData WHERE competitorname = 'Test Candy';
```

```sql
UPDATE CandyData SET winpercent = 80.0 WHERE competitorname = 'Test Candy';
```

```sql
DELETE FROM CandyData WHERE competitorname = 'Test Candy';
```

```sql
SELECT * FROM CandyData WHERE chocolate = 1 LIMIT 1;
```

```sql
INSERT INTO CandyData (competitorname, chocolate, fruity, caramel, peanutyalmondy, nougat, crispedricewafer, hard, bar, pluribus, sugarpercent, pricepercent, winpercent) VALUES ('Test Candy', 1, 0, 0, 0, 0, 1, 0, 1, 0, 0.75, 0.5, 60.0);
```

```sql
SELECT * FROM CandyData WHERE competitorname = 'Test Candy';
```

```sql
UPDATE CandyData SET winpercent = 80.0 WHERE competitorname = 'Test Candy';
```

```sql
DELETE FROM CandyData WHERE competitorname = 'Test Candy';
```

```sql
SELECT * FROM CandyData WHERE chocolate = 1 LIMIT 1;
```

```sql
SELECT * FROM CandyData WHERE chocolate = 1 LIMIT 1;
```

```sql
INSERT INTO CandyData (competitorname, chocolate, fruity, caramel, peanutyalmondy, nougat, crispedricewafer, hard, bar, pluribus, sugarpercent, pricepercent, winpercent) VALUES ('Test Candy', 1, 0, 0, 0, 0, 1, 0, 1, 0, 0.75, 0.5, 60.0);
```

```sql
SELECT * FROM CandyData WHERE competitorname = 'Test Candy';
```

```sql
UPDATE CandyData SET winpercent = 80.0 WHERE competitorname = 'Test Candy';
```

```sql
DELETE FROM CandyData WHERE competitorname = 'Test Candy';
```

